language: python
python:
    - "3.4"

install:
    - "pip install -r requirements/common.txt"
    - "pip install coveralls"

script: "coverage run --omit='settings/*','*/migrations/*' --source='.' manage.py test --settings=settings.test"

after_success:
    - "coveralls"
